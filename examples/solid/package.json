{
  "name": "@examples/solid",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "bun run --filter @spotify-effect/core build && bun run --filter @spotify-effect/browser build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "effect": "4.0.0-beta.36",
    "solid-js": "^1.9.3",
    "@spotify-effect/browser": "workspace:*",
    "@spotify-effect/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
